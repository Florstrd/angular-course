import { SetTextColor } from './set-text-color';

describe('SetTextColor', () => {
  it('should create an instance', () => {
    const directive = new SetTextColor();
    expect(directive).toBeTruthy();
  });
});
