import { RandomTextColor } from './random-text-color';

describe('RandomTextColor', () => {
  it('should create an instance', () => {
    const directive = new RandomTextColor();
    expect(directive).toBeTruthy();
  });
});
